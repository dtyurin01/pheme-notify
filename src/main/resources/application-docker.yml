spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/${DB_NAME:notification_hub}
    username: ${DB_USER:pheme}
    password: ${DB_PASSWORD}

  data:
    redis:
      host: redis
      port: 6379

  kafka:
    bootstrap-servers: kafka:29092

  mail:
    host: mailpit
    port: 1025
