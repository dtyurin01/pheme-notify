apiVersion: 1

providers:
  - name: pheme-notify
    orgId: 1
    folder: "Pheme Notify"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 300
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards